services:
  - type: web
    name: coffee-landing
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: coffee-data
      mountPath: /data
      sizeGB: 1
